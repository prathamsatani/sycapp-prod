services:
  - type: web
    name: box-cricket-manager
    runtime: node
    plan: free
    buildCommand: npm install --production=false && npm run build
    startCommand: npm run db:push && npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
